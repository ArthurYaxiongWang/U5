<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.title, product_path(product) %> - <%= product.category.name %> - <%= product.price %> - <%= product.stock_quantity %>
    </li>
  <% end %>
</ul>